// Simple in-memory storage for serverless deployment
// No database required - all data stored in session/localStorage on client

// This file exists for compatibility but doesn't use any database
export const db = null;
